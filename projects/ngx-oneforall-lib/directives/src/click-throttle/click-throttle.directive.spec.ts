import { ClickThrottleDirective } from './click-throttle.directive';

describe('ClickThrottleDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickThrottleDirective();
    expect(directive).toBeTruthy();
  });
});
