import { VisibilityChangeDirective } from './visibility-change.directive';

describe('VisibilityChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibilityChangeDirective();
    expect(directive).toBeTruthy();
  });
});
